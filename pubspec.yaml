name: galli_map
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: '>=2.12.0<3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.0
  flutter_svg: ^0.23.0
  flutter_map: ^1.1.1
  latlong2: ^0.8.1
  geolocator: ^7.7.0
  flutter_compass: ^0.7.0



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
